NODE_ENV=development
PORT=3000
# For local dev (non-Docker), use localhost. For Docker, use 'database' as hostname.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/atelier2
CLIENT_URL=http://localhost:5174
